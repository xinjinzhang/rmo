{
  "name": "rmo",
  "version": "1.0.0",
  "description": "delete objective-c *.a libaries duplicate symbol and assemble a new libary file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "rmo": "./bin/rmo.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "make-dir": "^3.1.0"
  }
}
